<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp">

    <TextView
        android:id="@+id/tvUpdateDialogTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="?attr/sib_text_primary"
        android:textSize="16sp"
        android:textStyle="bold"
        tools:text="Загрузка обновления v1.2..." />

    <TextView
        android:id="@+id/tvUpdateDialogStatus"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="Подготовка к скачиванию APK..."
        android:textColor="?attr/sib_text_secondary"
        android:textSize="13sp" />

    <com.google.android.material.progressindicator.LinearProgressIndicator
        android:id="@+id/pbUpdateDialog"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:indeterminate="false"
        android:max="100"
        android:progress="0"
        app:indicatorColor="@color/sib_blue_primary"
        app:trackColor="?attr/sib_border_subtle"
        app:trackCornerRadius="4dp" />

    <TextView
        android:id="@+id/tvUpdateDialogBytes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:layout_marginTop="8dp"
        android:textColor="?attr/sib_text_secondary"
        android:textSize="12sp"
        tools:text="0% (0 / 18.5 МБ)" />

</LinearLayout>
